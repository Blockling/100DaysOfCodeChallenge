#Program that will take a two digit number and add its digits together

tDnumber = input("Enter a two digit number: ")		#take two digit number (as string)

FinalNumber = int(tDnumber[0])+int(tDnumber[1])		#add the digits together (as an integer)

print(FinalNumber)									#print the result